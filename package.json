{
  "name": "devops-app",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "nodemon --watch 'src/**/*.js' --ignore 'src/**/*.spec.js' --exec 'node src/index.js'",
    "lint": "npx eslint ./**/*.js",
    "lint:fix": "npx eslint ./**/*.js --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.7",
    "prettier": "2.2.1"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
